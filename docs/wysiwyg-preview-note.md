# Theme Editor WYSIWYG Preview Contract

The Theme Editor live preview uses the same `buildThemeCss()` generator and the same production `.sm-*` markup contract used by generated Group Multiplied resources. The iframe may use sample content, but its layout, typography, spacing, colors, cards, logo treatment, and structural theme rules must come from the production CSS path rather than a separate mock stylesheet.
